# DO NOT REMOVE. GENERATED BY Node.js App
# This serves as a fallback .htaccess if you run Node.js on a manual port like PM2.
# If you are using cPanel's "Setup Node.js App", Passenger will handle configuration automatically.

<IfModule mod_rewrite.c>
    RewriteEngine On
    
    # Optional: If you are running Node on a specific port (e.g., 5000), use Proxy. 
    # Uncomment the proxy rules below if Passenger is NOT installed/used.
    
    # Proxy API requests to Node server
    # RewriteCond %{REQUEST_URI} ^/api [OR]
    # RewriteCond %{REQUEST_URI} ^/uploads
    # RewriteRule ^(.*)$ http://127.0.0.1:5000/$1 [P,L]

    # Optional: Route all other requests to the React SPA index.html (if served by Apache instead of Express)
    # RewriteCond %{REQUEST_FILENAME} !-f
    # RewriteCond %{REQUEST_FILENAME} !-d
    # RewriteRule ^(.*)$ /index.html [QSA,L]
</IfModule>
