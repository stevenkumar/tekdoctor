# TekDoctor Frontend — Development Environment Variables
# API base URL: Empty in development to use the Vite dev server proxy (handles /api/* → http://localhost:5000)
VITE_API_URL=https://tekunik.vibgyorventures.co.in
VITE_APP_ENV=development
