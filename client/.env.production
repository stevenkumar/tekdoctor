# TekDoctor Frontend — Production Environment Variables
# API base URL for live cPanel deployment
# Left empty purposely to use relative paths so it seamlessly inherits whatever domain it is deployed on!
VITE_API_URL=
VITE_APP_ENV=production
